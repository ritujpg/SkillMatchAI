import { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import { Check, Logo, LoadingState } from "@/components/skillmatch";
const stages = ["Reading your resume", "Comparing job requirements", "Identifying skill gaps", "Generating recommendations"];
export default function Analyzing() { const navigate = useNavigate(); const [stage, setStage] = useState(0); useEffect(() => { if (stage === stages.length) { const timer = window.setTimeout(() => navigate("/results"), 550); return () => window.clearTimeout(timer); } const timer = window.setTimeout(() => setStage((current) => current + 1), 800); return () => window.clearTimeout(timer); }, [stage, navigate]); return <div className="min-h-screen bg-slate-50"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex h-16 max-w-6xl items-center px-4 sm:px-6"><Logo /></div></header><main className="grid min-h-[calc(100vh-65px)] place-items-center px-4"><section className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-7 text-center shadow-[0_15px_45px_-25px_rgba(15,23,42,.3)] sm:p-9"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-blue-50 text-blue-600"><LoadingState /></div><h1 className="mt-6 text-2xl font-bold tracking-tight text-slate-950">Analyzing your match...</h1><p className="mt-2 text-sm text-slate-600">We’re mapping your experience to this opportunity.</p><div className="mt-8 space-y-4 text-left">{stages.map((item, index) => <div key={item} className="flex items-center gap-3 text-sm"><span className={`grid h-6 w-6 place-items-center rounded-full ${index < stage ? "bg-emerald-500 text-white" : index === stage ? "bg-blue-600 text-white" : "bg-slate-100 text-slate-400"}`}>{index < stage ? <Check className="h-3.5 w-3.5" /> : index === stage ? <LoadingState /> : index + 1}</span><span className={index <= stage ? "font-medium text-slate-800" : "text-slate-400"}>{item}</span></div>)}</div><div className="mt-8 h-1.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-600 transition-all duration-500" style={{ width: `${Math.min((stage / stages.length) * 100, 100)}%` }} /></div></section></main></div>; }
